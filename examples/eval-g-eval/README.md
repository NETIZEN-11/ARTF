# eval-g-eval (G Eval)

You can run this example with:

```bash
npx artef@latest init --example eval-g-eval
cd eval-g-eval
```

## Usage

This example shows how you can use [G-Eval](https://arxiv.org/abs/2303.16634) with artef

Configure in `artefconfig.yaml`. Run with:

```bash
artef eval
```
