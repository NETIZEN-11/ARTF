# compare-llama-vs-gpt (Llama Gpt Comparison)

You can run this example with:

```bash
npx artef@latest init --example compare-llama-vs-gpt
cd compare-llama-vs-gpt
```

## Usage

To get started, set your OPENAI_API_KEY and REPLICATE_API_TOKEN environment variables.

Next, change a few of the prompts in prompts.txt and edit artefconfig.yaml.

Then run:

```bash
artef eval
```

Afterwards, you can view the results by running `artef view`
