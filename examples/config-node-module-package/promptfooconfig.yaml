# yaml-language-server: $schema=https://artef.dev/config-schema.json
description: 'Node modules'

prompts:
  - 'Write a tweet about {{topic}}'

providers:
  - package:testpackage:Provider

tests:
  - vars:
      topic: bananas
