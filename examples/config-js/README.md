# config-js (JavaScript Configuration)

You can run this example with:

```bash
npx artef@latest init --example config-js
cd config-js
```

## Usage

This example is pre-configured in `artefconfig.js`. That means you can just run:

```bash
artef eval
```
