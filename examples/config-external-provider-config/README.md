# config-external-provider-config (External Provider Config)

You can run this example with:

```bash
npx artef@latest init --example config-external-provider-config
cd config-external-provider-config
```

## Usage

This example is pre-configured in `artefconfig.yaml`. That means you can just run:

```bash
artef eval
```
