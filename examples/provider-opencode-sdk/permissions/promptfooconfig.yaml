# yaml-language-server: $schema=https://artef.dev/config-schema.json
description: 'OpenCode SDK session-level permission rules'

prompts:
  - 'Summarize in one sentence what provider_id is configured in the local artefconfig.yaml.'

providers:
  - id: opencode:sdk
    config:
      provider_id: openai
      model: gpt-4o-mini
      working_dir: ../basic
      # Exercise both simple and pattern-based permission shapes.
      # artef converts these into the ruleset the OpenCode v2 API expects.
      permission:
        read: allow
        grep: allow
        glob: allow
        list: allow
        edit: deny
        bash:
          '*': deny
          'git status': allow
        webfetch: deny
        external_directory: deny

tests:
  - vars: {}
    assert:
      - type: contains
        value: openai
