# config-retry-testing (Retry Testing)

You can run this example with:

```bash
npx artef@latest init --example config-retry-testing
cd config-retry-testing
```

## Usage

This example creates a test harness with a high error rate (~50%) to test the retry functionality using `artef eval --retry-errors`.
