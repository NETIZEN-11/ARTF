# config-nunjucks-custom-filters (Nunjucks Custom Filters)

You can run this example with:

```bash
npx artef@latest init --example config-nunjucks-custom-filters
cd config-nunjucks-custom-filters
```

## Usage

This example is pre-configured in `artefconfig.yaml`. That means you can just run:

```bash
artef eval
```

`allcaps.js` is a standard [Nunjucks custom filter](https://mozilla.github.io/nunjucks/api.html#custom-filters).
