apiVersion: v1
kind: Service
metadata:
  name: {{ include "artef.fullname" . }}
  labels:
    {{- include "artef.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "artef.selectorLabels" . | nindent 4 }}
